# Run Output Directory

Each generated theme run should create a folder here named after the slug, for example `001_nolan_young_theme_meridian_strategy_group/`.

The workflow writes plans, raw model output, validation output, and run metadata here so failures can be debugged after the fact.

