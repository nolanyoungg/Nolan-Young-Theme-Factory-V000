# Accessibility Notes

- Use semantic headings and landmark regions.
- Keep focus states visible.
- Preserve readable contrast on buttons, cards, and footer links.
- Respect `prefers-reduced-motion` where possible in future iterations.

