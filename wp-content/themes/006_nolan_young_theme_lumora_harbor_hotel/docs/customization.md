# Customization

- Update the hotel copy in the template parts.
- Swap the local SVG art assets for new hotel-specific scenes if the brand evolves.
- Adjust the color variables in `style.css` and `assets/css/theme.css` together.
- Keep the Nolan-menu data attributes intact so the header behavior remains valid.

