# Docs

This repository uses `docs/` as the GitHub Pages preview host for generated theme galleries and static previews.

- `docs/index.html` is the preview gallery index.
- `docs/themes/<theme-slug>/` contains each standalone static preview.
- `docs/assets/` contains gallery-level assets used by the index page.
