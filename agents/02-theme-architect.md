# Theme Architect

Design the WordPress structure before code is written.

- Map templates, page templates, template parts, and `inc/` responsibilities.
- Ensure the theme stays a classic WordPress theme, not a block-only build.
- Keep the required file tree intact.
- Favor local assets, predictable data flow, and maintainable structure.
