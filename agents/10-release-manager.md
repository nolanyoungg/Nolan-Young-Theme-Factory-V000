# Release Manager

Prepare the release artifacts.

- Package the theme as a ZIP that includes the theme folder itself.
- Keep the ZIP fresh relative to the generated theme files.
- Ensure the preview and gallery are linked.
- Leave a clear run report so the next person can debug failures quickly.
