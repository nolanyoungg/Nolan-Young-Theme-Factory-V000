# Static Preview Template Notes

Generated previews are created directly in `docs/themes/<theme-slug>/` and must satisfy `contracts/required-preview-structure.md`.

The preview must work on GitHub Pages without WordPress, PHP, APIs, remote scripts, external image hosts, or CDN CSS.

This directory is reserved for future reusable preview seed assets. The current workflow relies on contracts and generated file blocks.
